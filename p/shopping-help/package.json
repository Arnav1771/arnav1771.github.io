{
  "name": "shopping-help",
  "version": "2.0.0",
  "private": true,
  "description": "Mummy's Shopping Helper — static Hinglish product advisor with a bring-your-own-key multi-provider LLM fallback chain.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check app.js && node --check js/app.js && node --check js/llm.js"
  }
}
